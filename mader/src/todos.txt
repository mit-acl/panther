
En este repo:
	Make sure qyaw_guess is feasible (because I commit to it when ipopt doesn't find a solution)
	Improve the way qyaw_guess is generated
	max_runtime_casadi
	Install good linear solver Ipopt


En el orginial MADER repo:

	Change to Release en el CMakeList.txt
	namespaces
	mode()
	std::unique ptr
	I think I should use remove
	update last version separator